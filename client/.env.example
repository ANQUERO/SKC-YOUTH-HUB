# Development proxy target. Vite serves the client at http://localhost:5173
# and forwards /api requests to this backend origin.
VITE_BACKEND_URL=http://localhost:4521

# For a production build, set this in the hosting provider instead:
# VITE_API_URL=https://your-backend.example.com
